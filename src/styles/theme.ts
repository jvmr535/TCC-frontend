import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0052F5",
      dark: "#0039AB",
    },
    secondary: {
      main: "#F5A900",
      dark: "#A87400",
    },
  },
});

export default theme;
